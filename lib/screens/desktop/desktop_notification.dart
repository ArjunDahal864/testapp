import 'package:flutter/material.dart';

class DesktopNotificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("notification desktop"),),
    );
  }
}