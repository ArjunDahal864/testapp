import 'package:flutter/material.dart';

class TabletNotificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("notification tablet"),),
    );
  }
}