import 'package:flutter/material.dart';

class TabletFavouritePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("favourite tablet"),),
    );
  }
}