import 'package:flutter/material.dart';

class TabletProfilePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
      return Scaffold(
      body: Center(child: Text("profile tablet"),),
    );
  }
}