import 'package:flutter/material.dart';

class MobileNotificationPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("notification mobile"),),
    );
  }
}